```bash
$ node proxy-server.js username password

$ export HTTP_PROXY=http://username:password@localhost:3000
$ export HTTPS_PROXY=http://username:password@localhost:3000

$ curl https://www.google.com
```